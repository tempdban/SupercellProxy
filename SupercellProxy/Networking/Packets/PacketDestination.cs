using System;

namespace SupercellProxy
{
    /// <summary>
    /// Specifies if a packet is client or server sided
    /// </summary>
    enum PacketDestination
    {
        FROM_CLIENT,
        FROM_SERVER
    }
}
